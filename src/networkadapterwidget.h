#pragma once

#include "deviceinfowidgetbase.h"

class NetworkadapterWidget : public DeviceInfoWidgetBase
{
    Q_OBJECT
public:
    explicit NetworkadapterWidget(QWidget *parent = nullptr);

signals:

public slots:
};

